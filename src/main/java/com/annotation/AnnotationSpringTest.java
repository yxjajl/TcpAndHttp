package com.annotation;


public class AnnotationSpringTest {

	public void addHandler() {
		// HandlerProcessor handlerProcessor = HandlerProcessor.getInstance();
		// Map<String, Object> map = ctx.getBeansWithAnnotation(Action.class);
		// for (Object obj : map.values()) {
		// int model = obj.getClass().getAnnotation(Action.class).value();
		// System.out.println(model);
		// handlerProcessor.addCommandHandler(model, (ICommandHandler) obj);
		// }
	}
}
